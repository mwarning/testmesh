menu "Configuration"
	depends on PACKAGE_geomesh-firmware

config GEOMESH_FIRMWARE_GATEWAY
	bool "Configure as gateway."
	depends on PACKAGE_geomesh-firmware
	default n

config GEOMESH_FIRMWARE_ALGORITHM
	string "Default routing algorithm."
	depends on PACKAGE_geomesh-firmware
	default "flood-0"

endmenu
